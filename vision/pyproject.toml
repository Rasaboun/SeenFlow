[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "seenflow-sidecar"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "numpy",
  "onnxruntime==1.28.0",
  "paddleocr==3.7.0",
  "paddlepaddle==3.3.0",
  "pillow",
  "uvicorn",
]

[project.optional-dependencies]
test = [
  "httpx2",
  "pytest",
]

[project.scripts]
seenflow-sidecar = "seenflow.server:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["seenflow"]

[tool.uv.sources]
paddlepaddle = { index = "paddle-cpu" }

[[tool.uv.index]]
name = "paddle-cpu"
url = "https://www.paddlepaddle.org.cn/packages/stable/cpu/"
explicit = true
